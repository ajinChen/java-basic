public class part5_varType {
    /* Primitive Variable (8 types)
     * 1. boolean: true/false
     * 2. char: 16 bits represent a single character
     * 3. byte: 8 bits [-2^7, 2^7-1]
     * 4. short: 16 bits [-2^15, 2^15-1]
     * 5. int: 32 bits [-2^31, 2^31-1]
     * 6. long: 64 bits [-2^63, 2^63-1]
     * 7. float: 32 bits floating-point number
     * 8. double: 64 bits floating-point number
     */

     /* Reference Variable
      * The most significant difference between primitive and reference variables is that 
      * primitives (usually numbers) are immutable. 
      * The internal state of reference variables, on the other hand, can typically be mutated. 
      */
}
